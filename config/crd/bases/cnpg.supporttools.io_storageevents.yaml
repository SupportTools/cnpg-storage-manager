---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: storageevents.cnpg.supporttools.io
spec:
  group: cnpg.supporttools.io
  names:
    kind: StorageEvent
    listKind: StorageEventList
    plural: storageevents
    shortNames:
    - se
    singular: storageevent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.eventType
      name: Type
      type: string
    - jsonPath: .spec.trigger
      name: Trigger
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          StorageEvent is the Schema for the storageevents API.
          It records storage-related events for audit trail.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageEventSpec defines the desired state of StorageEvent
            properties:
              clusterRef:
                description: ClusterRef references the CNPG cluster this event relates
                  to
                properties:
                  name:
                    description: Name of the CNPG cluster
                    type: string
                  namespace:
                    description: Namespace of the CNPG cluster
                    type: string
                required:
                - name
                - namespace
                type: object
              dryRun:
                default: false
                description: DryRun indicates this is a dry-run event
                type: boolean
              eventType:
                description: EventType is the type of storage event
                enum:
                - expansion
                - wal-cleanup
                - alert
                - circuit-breaker
                type: string
              expansion:
                description: Expansion contains details for expansion events
                properties:
                  affectedPVCs:
                    description: AffectedPVCs is the list of PVCs being expanded
                    items:
                      description: AffectedPVC represents a PVC affected by an expansion
                        event
                      properties:
                        name:
                          description: Name of the PVC
                          type: string
                        node:
                          description: Node where the PVC is mounted
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  originalSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: OriginalSize is the size before expansion
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  requestedSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: RequestedSize is the requested new size
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - originalSize
                - requestedSize
                type: object
              policyRef:
                description: PolicyRef references the StoragePolicy that triggered
                  this event
                properties:
                  name:
                    description: Name of the StoragePolicy
                    type: string
                  namespace:
                    description: Namespace of the StoragePolicy
                    type: string
                required:
                - name
                - namespace
                type: object
              reason:
                description: Reason explains why this event was triggered
                type: string
              trigger:
                description: Trigger is what triggered this event
                enum:
                - threshold-breach
                - manual
                - scheduled
                - automatic
                type: string
              walCleanup:
                description: WALCleanup contains details for WAL cleanup events
                properties:
                  filesRemoved:
                    description: FilesRemoved is the number of WAL files removed
                    format: int32
                    type: integer
                  oldestRetained:
                    description: OldestRetained is the name of the oldest retained
                      WAL segment
                    type: string
                  podName:
                    description: PodName is the name of the pod where WAL cleanup
                      was performed
                    type: string
                  spaceFreedBytes:
                    description: SpaceFreedBytes is the amount of space freed in bytes
                    format: int64
                    type: integer
                type: object
            required:
            - clusterRef
            - eventType
            - trigger
            type: object
          status:
            description: StorageEventStatus defines the observed state of StorageEvent
            properties:
              completionTime:
                description: CompletionTime is when the event completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the event
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message provides additional details about the current
                  status
                type: string
              nextRetryTime:
                description: NextRetryTime is when the next retry will be attempted
                format: date-time
                type: string
              phase:
                default: Pending
                description: Phase is the current phase of the event
                enum:
                - Pending
                - InProgress
                - Completed
                - Failed
                type: string
              pvcStatuses:
                description: PVCStatuses contains per-PVC status for expansion events
                items:
                  description: PVCStatus represents the status of a single PVC operation
                  properties:
                    error:
                      description: Error message if the operation failed
                      type: string
                    filesystemResized:
                      description: FilesystemResized indicates if the filesystem was
                        resized
                      type: boolean
                    name:
                      description: Name of the PVC
                      type: string
                    newSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: NewSize is the size after operation
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    originalSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: OriginalSize is the size before operation
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    phase:
                      description: Phase of the PVC operation
                      enum:
                      - Pending
                      - InProgress
                      - Completed
                      - Failed
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
              retryCount:
                description: RetryCount is the number of retry attempts
                format: int32
                type: integer
              startTime:
                description: StartTime is when the event started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
